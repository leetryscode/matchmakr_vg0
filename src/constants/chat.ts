/**
 * Chat scroll constants for consistent behavior across all chat surfaces
 * (sponsor page, single page, ChatModal).
 */

/** Distance from bottom (px) to consider user "pinned" / "near bottom" for auto-scroll. */
export const SCROLL_PIN_THRESHOLD_PX = 64;
