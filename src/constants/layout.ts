/**
 * Layout constants for consistent spacing and sizing across the application.
 */

/**
 * Height of the bottom navigation bar in pixels.
 * This is used as the single source of truth for bottom nav height calculations.
 */
export const BOTTOM_NAV_HEIGHT_PX = 72;

